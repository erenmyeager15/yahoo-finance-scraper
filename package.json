{
  "name": "yahoo-finance-scraper",
  "version": "1.0.0",
  "description": "Apify Actor that scrapes Yahoo Finance stock and crypto quotes plus historical OHLCV via the public chart API",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "keywords": ["apify", "yahoo-finance", "stocks", "crypto", "finance", "scraper", "crawlee"],
  "license": "Apache-2.0",
  "dependencies": {
    "apify": "^3.2.0",
    "crawlee": "^3.11.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
